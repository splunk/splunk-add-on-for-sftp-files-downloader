[sftp_files_downloader://<name>]
ssh_server = 
port = 
username = Username for SSH access
password = Password for SSH access (not required if using private key authentication)
key_file_type = 
key_file_path = Location of the key file (not required if using username and password authentication)
ssh_server_known_hosts_path = Location of the known hosts file (not required if using username and password authentication)
server_file_path = The server file path for files to be downloaded
burn_after_download = Remove files on the server after downloading
destination_file_path = Local path to save the files to